pre-commit:
  parallel: true
  commands:
    analyze:
      glob: '*.{dart}'
      run: dart run halildurmus_hooks:analyze --flutter example lib test
    format:
      glob: '*.{dart}'
      run: dart run halildurmus_hooks:format {staged_files}
    test:
      glob: '{lib,test}/**/*.dart'
      run: dart run halildurmus_hooks:test --flutter

commit-msg:
  commands:
    check_commit:
      run: dart run halildurmus_hooks:check_commit {1}
