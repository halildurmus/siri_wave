name: siri_wave
description: A new Flutter package project.
version: 0.0.1
homepage: https://pub.dev/packages/siri_wave
repository: https://github.com/halildurmus/siri_wave

environment:
  sdk: ">=2.14.0 <3.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^1.0.0
  flutter_test:
    sdk: flutter

flutter: